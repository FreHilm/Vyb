---
id: T-016
title: Parallel agents — UI fixes
status: done
assignee: null
priority: null
tags: [bug, ui, parallel-agents, sidebar]
depends_on: []
created_at: 2026-04-26
updated_at: 2026-04-26
---

## Goal

When running multiple agents, spawning a new parallel agent should automatically focus its row. Each parallel agent should also have its own independent overlay state — whether Kanban, README, or the terminal is shown should not be shared across agents.

## Acceptance Criteria

## Notes

## Progress

- 2026-04-26: Confirmed scope with user. Plan: re-key overlay sets (readme/files/kanban) by composite view key (`profileId` for parent, `profileId|parallelId` for parallel views) so each agent has independent overlay visibility. README/Files/Kanban pointed at the parallel agent's worktree when viewing a parallel. OrdnaManager refactored to support multiple instances per profile keyed by an opaque instanceKey + cwd, so each parallel can run its own Ordna over its worktree. Auto-focus on spawn already works once overlays are per-view.
- 2026-04-26: Implemented. Files touched: `src/main/ordna-manager.ts` (instances now keyed by instanceKey + carry profileId/cwd; added stopForProfile helper), `src/main/ipc-handlers.ts` (ORDNA_START takes instanceKey/profileId/cwd, ORDNA_EXITED emits instanceKey, ordna PTY exit uses instanceKey), `src/preload.ts` (signatures updated), `src/renderer/components/KanbanViewer.tsx` (props changed to instanceKey/profileId/cwd), `src/renderer/App.tsx` (renamed sets to readmeViews/filesViews/kanbanViews; activeViewKey + activeViewCwd derived; toggles + cleanup paths use viewKey; ReadmeViewer/FileExplorer use worktree path when a parallel agent is selected; spawning a parallel selects its row and its empty per-view overlay state means terminal becomes visible immediately; cleanup on parallel exit + profile stop/delete also stops associated Ordna instances). `tsc --noEmit` passes.
